# Blueprint Render (gratuit) — Black Panther
# https://render.com/docs/blueprint-spec
databases:
  - name: blackpanther-db
    plan: free
    databaseName: blackpanther
    user: blackpanther

services:
  # BACKEND — Web Service Python (dossier backend/)
  - type: web
    name: blackpanther-api
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: chmod +x build.sh start.sh && ./build.sh
    startCommand: chmod +x start.sh && ./start.sh
    healthCheckPath: /api/health/
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.7"
      - key: DATABASE_URL
        fromDatabase:
          name: blackpanther-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "false"
      - key: RENDER
        value: "true"
      - key: CELERY_TASK_ALWAYS_EAGER
        value: "true"
      - key: DJANGO_SUPERUSER_USERNAME
        value: blackpanther
      - key: DJANGO_SUPERUSER_PASSWORD
        sync: false
      - key: DJANGO_SUPERUSER_EMAIL
        value: owner@blackpanther.app
      - key: CORS_ALLOWED_ORIGINS
        sync: false
      - key: ALLOWED_HOSTS
        sync: false

  # FRONTEND — Static Site (dossier frontend/)
  - type: web
    name: blackpanther-web
    runtime: static
    plan: free
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        sync: false
